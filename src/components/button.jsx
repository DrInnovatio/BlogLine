import * as React from 'react';
import Button from '@mui/material/Button';

export default function SubmitButton() {
  return (
    <Button variant="contained" style={{width: '15rem'}}>
      제출
    </Button>
  );
}